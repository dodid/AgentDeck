{
  "name": "r2-relay-core",
  "version": "0.1.0",
  "description": "Host-agnostic TypeScript shared transport core for the R2 relay",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ajv": "^8.17.1",
    "typescript": "^7.0.2"
  }
}
